# ── Agent Core & APIs ────────────────────────────────────────
requests
python-dotenv
openai

# ── Auth (JWT + Hachage mot de passe) ────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
groq

# ── Agent Voice & Audio (TTS / STT) ──────────────────────────
edge-tts
pygame
openai-whisper
sounddevice

# ── RAG (Retrieval-Augmented Generation) ─────────────────────
sentence-transformers
chromadb

# ── Computer Vision & Pose (CV Module) ───────────────────────
# Evaluation (CV / Emotion / Body)
opencv-python>=4.9.0
mediapipe>=0.10.13
scikit-learn

# ── Facial Emotion Recognition ───────────────────────────────
# deepface 0.0.93 works with TF 2.21 + tf-keras shim
deepface==0.0.93
fer==22.5.1

# ── Audio / Tone Analysis ────────────────────────────────────
# PyAudio 0.2.14 is the last version compatible with cffi <2.0 on Windows
PyAudio==0.2.14
librosa==0.10.1
speechbrain==1.0.2

# ── Deep Learning ────────────────────────────────────────────
# torch 2.6.0 + torchvision 0.21.0 are a matched stable pair for Python 3.12
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0
# timm 0.9.16 — EfficientNet-B2 emotion backend (compatible with torch 2.6.0)
timm==0.9.16
hsemotion==0.3.0
huggingface-hub>=0.21.0
transformers>=4.39.3
# Use onnxruntime (CPU) or onnxruntime-gpu if CUDA is available
onnxruntime==1.17.3

# ── Data & Utilities ─────────────────────────────────────────
# numpy MUST stay on 1.x — mediapipe, librosa, numba all break on numpy 2.x
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1

# ── Sub-dependency pins (prevent pip backtracking) ───────────
# cffi 2.0 is incompatible with PyAudio and several audio libs
cffi==1.17.1
# soundfile and soxr for librosa
soundfile==0.12.1
soxr==0.3.7
six==1.16.0
setuptools==69.5.1

# ── Dashboard & Reporting ────────────────────────────────────
streamlit==1.33.0
plotly==5.22.0
fpdf2==2.8.2

# ── TF / Keras Compatibility ─────────────────────────────────
# Use newer TF for Python 3.12 compatibility
tensorflow>=2.16.1
tf-keras
protobuf>=4.25.3
